[project]
name = "malo-scp"
version = "0.1.0"
description = "Hy-like Lisp interpreter"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
malo = "malo.cli:main"

[build-system]
requires = ["uv_build>=0.12,<0.13"]
build-backend = "uv_build"
